#include <stdio.h>

int main(void) {
  int numero[10];
  int i;
  int x;
  for(i=0 ; i< 10; i++){
    scanf("%d", &numero[i]);
  }

  for(i=0 ; i< 10; i++){
  printf("%d\n", numero[i]);
  }
  //quais são diviseveis por x e fazer um vetor q armazene os resultados de tal(só mostrar quais são diviseveis)
  return 0;
}
